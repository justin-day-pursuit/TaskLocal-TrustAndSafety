# TEST.md

> Running ledger of generated tests for regression + documentation. Owned by the verification
> layer (`critic`, `output-validator`, `safety-guardrail`). One section per task-id.

## <task-id> — <feature>
- Integration tests:
- Edge cases:
- Coverage gaps identified:
- Untested code paths flagged:
